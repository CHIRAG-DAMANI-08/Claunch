---
phase: 04-claude-&-terminal-launching
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified: [src/claude/__tests__/launchClaude.test.ts, src/terminal/__tests__/openWindowsTerminal.test.ts]
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-04, TERM-05, TERM-06, TERM-07, TERM-08]

must_haves:
  truths:
    - "Claude command builder tests verify resume and fallback cases"
    - "Terminal launcher tests verify correct wt.exe arguments structure"
  artifacts:
    - src/claude/__tests__/launchClaude.test.ts
    - src/terminal/__tests__/openWindowsTerminal.test.ts
---

<objective>
Write unit tests for Claude command builder and Windows Terminal launcher.

Purpose: Guarantee correct CLI command generation for terminal tab launching.
Output: src/claude/__tests__/launchClaude.test.ts and src/terminal/__tests__/openWindowsTerminal.test.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for launcher modules</name>
  <files>src/claude/__tests__/launchClaude.test.ts, src/terminal/__tests__/openWindowsTerminal.test.ts</files>
  <action>
  Implement src/claude/__tests__/launchClaude.test.ts testing `buildCommand` outputs.
  Implement src/terminal/__tests__/openWindowsTerminal.test.ts mocking `child_process.spawn` to verify argument arrays.
  Check that:
  - `-w -1` is present to force new window.
  - `-d` and `--title` are correctly set per tab.
  - Semicolon delimiters `;` and `new-tab` arguments are built correctly.
  - Powershell launch commands are correctly embedded.
  </action>
  <verify>npx vitest run src/claude/ src/terminal/ shows all tests passing</verify>
  <done>Launcher tests pass</done>
</task>

</tasks>
