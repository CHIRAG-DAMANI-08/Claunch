---
phase: 06-packaging-&-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsup.config.ts]
autonomous: true
requirements: [PKG-01, PKG-03, PKG-04]

must_haves:
  truths:
    - "tsup configuration outputs cli executable with shebang"
    - "package.json defines bin field matching dist/cli.js"
  artifacts:
    - package.json
    - tsup.config.ts
---

<objective>
Configure the final npm bundling and execution entry configurations.

Purpose: Allow clean global installation via `npm install -g claunch`.
Output: Updates to package.json and tsup.config.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Verify packaging config</name>
  <files>package.json, tsup.config.ts</files>
  <action>
  Review package.json and tsup.config.ts:
  - Verify package.json contains `"type": "module"`, `"bin": { "claunch": "./dist/cli.js" }`, and `"main": "./dist/index.js"`.
  - Verify tsup.config.ts entry is `src/cli.ts` (which exposes `runCli`), target is `node22`, format is `esm`, and shebang banner `#!/usr/bin/env node` is attached to generated file.
  - Run `npm run build` to verify bundling succeeds.
  </action>
  <verify>npm run build succeeds and dist/cli.js starts with #!/usr/bin/env node</verify>
  <done>Bundling configurations are correct and output is executable</done>
</task>

</tasks>
