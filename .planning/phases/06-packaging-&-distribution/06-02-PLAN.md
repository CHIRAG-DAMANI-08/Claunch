---
phase: 06-packaging-&-distribution
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified: []
autonomous: true
requirements: [PKG-01, PKG-03, PKG-04]

must_haves:
  truths:
    - "claunch installs globally and runs successfully from the command line"
  artifacts: []
---

<objective>
Verify global installation and end-to-end execution on the system.

Purpose: Guarantee that global cli command performs correctly in real scenarios.
Output: Validated global installation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: E2E global installation test</name>
  <files></files>
  <action>
  Link the package locally using `npm link` to simulate global installation.
  Run `claunch` from the command line inside the repo to verify it attempts to discover worktrees and launch wt.exe.
  Note: This will spawn Windows Terminal. If mock tools are not available, it should output any errors cleanly or succeed.
  Since we are running inside our git repository, running `claunch` will discover 1 worktree (the repository itself), write mapping to `~/.claunch/sessions.json`, and open a new Windows Terminal tab running claude.
  Let's verify this completes without throwing unhandled exceptions.
  </action>
  <verify>claunch --help or running claunch succeeds</verify>
  <done>E2E verification succeeds</done>
</task>

</tasks>
