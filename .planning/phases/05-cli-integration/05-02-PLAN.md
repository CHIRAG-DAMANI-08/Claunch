---
phase: 05-cli-integration
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified: [src/__tests__/integration.test.ts]
autonomous: true
requirements: [GIT-06, TERM-05]

must_haves:
  truths:
    - "Integration tests run cli.ts workflow with mocked dependencies"
  artifacts:
    - src/__tests__/integration.test.ts
---

<objective>
Write integration tests for the full CLI execution.

Purpose: Validate orchestration, fallback mappings, and sorting under integration.
Output: src/__tests__/integration.test.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests</name>
  <files>src/__tests__/integration.test.ts</files>
  <action>
  Implement src/__tests__/integration.test.ts.
  Mock the underlying utility functions and commands (git worktree lists, environment checks, wt spawn) using vitest mock.
  Run the Commander action workflow and assert:
  - Mappings are written to SessionStore when missing.
  - Windows Terminal is opened with correct tabs.
  - Tab ordering puts current worktree first.
  - Error messages are output correctly.
  </action>
  <verify>npm run test shows all tests passing</verify>
  <done>Integration tests pass</done>
</task>

</tasks>
