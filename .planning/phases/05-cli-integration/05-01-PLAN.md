---
phase: 05-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/cli.ts]
autonomous: true
requirements: [GIT-06, TERM-05]

must_haves:
  truths:
    - "cli.ts orchestrates git, session store, and wt launching modules"
    - "cli.ts handles errors and displays exact PRD error messages"
  artifacts:
    - src/cli.ts
---

<objective>
Wire up all modules inside cli.ts for end-to-end execution.

Purpose: Implement the primary execution flow of claunch CLI.
Output: src/cli.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate CLI logic</name>
  <files>src/cli.ts</files>
  <action>
  Implement src/cli.ts.
  Use Commander.js to define the command.
  In the action:
  - Run `validateEnvironment()` to check requirements and get the repo root.
  - If a check throws a ClaunchError, print its message to stderr and exit with code 1.
  - Run `discoverWorktrees(process.cwd())` to get the sorted list of active worktrees.
  - Instantiate `SessionStore`.
  - For each worktree:
    - Query `SessionStore` for a mapped session using repo root and worktree branch name.
    - If a mapping is found, use it. Otherwise, use branch name as mapping (PRD says: 'If no mapping exists: claude... After first successful launch, remember the mapping forever'. Since we cannot wait for Claude's outcome from within the detached WT session, we should proactively remember the branch mapping.
    Actually, let's write to SessionStore: if `getSession` returns null, write `setSession(repoRoot, branch, branch)` to remember the mapping.
    - Build the Claude command string via `buildClaudeCommand`.
    - Map worktree to `TabSpec`.
  - Pass the array of `TabSpec`s to `openWindowsTerminal`.
  - Exit 0.
  </action>
  <verify>npx tsc --noEmit compiles cleanly</verify>
  <done>cli.ts compiles and wires modules</done>
</task>

</tasks>
