---
phase: 01-project-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/environment.ts, src/utils/__tests__/environment.test.ts]
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03, ERR-04]

must_haves:
  truths:
    - "checkGit() detects missing git and returns clear error"
    - "checkClaude() detects missing claude CLI and returns clear error"
    - "checkWindowsTerminal() detects missing wt.exe and returns clear error"
    - "checkGitRepo() detects not-a-repo and returns clear error"
  artifacts:
    - src/utils/environment.ts
    - src/utils/__tests__/environment.test.ts
  key_links:
    - "Environment checks use child_process.execSync to test command availability"
---

<objective>
Implement environment validation utilities that check for required tools.

Purpose: Provide clear, actionable error messages when prerequisites are missing.
Output: src/utils/environment.ts with functions to validate git, claude, wt.exe, and repo detection.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement environment validation functions</name>
  <files>src/utils/environment.ts</files>
  <action>
  Create src/utils/environment.ts with:

  - checkGit(): Runs `git --version` via execSync. Returns void on success, throws ClaunchError with message "Git is not installed." on failure.
  - checkClaude(): Runs `claude --version` via execSync with { shell: true } (for Windows .cmd resolution). Returns void on success, throws ClaunchError with "Claude Code CLI not found." on failure.
  - checkWindowsTerminal(): Runs `where wt` via execSync. Returns void on success, throws ClaunchError with "Windows Terminal is required." on failure.
  - checkGitRepo(): Runs `git rev-parse --show-toplevel` via execSync. Returns the repo root path on success, throws ClaunchError with "No Git repository detected." on failure.
  - validateEnvironment(): Calls all checks in sequence, returns repo root.

  Use { stdio: 'pipe' } to suppress output. Use try/catch around execSync.
  </action>
  <verify>npx tsc --noEmit succeeds</verify>
  <done>Environment validation functions compile and handle errors</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for environment validation</name>
  <files>src/utils/__tests__/environment.test.ts</files>
  <action>
  Create tests that verify:
  - checkGit returns void when git is available (it should be on this system)
  - checkGitRepo throws "No Git repository detected." when run outside a repo (mock execSync)
  - Error messages match exactly the PRD-specified strings
  
  Use vitest mock to mock child_process.execSync for failure cases.
  </action>
  <verify>npx vitest run shows all tests passing</verify>
  <done>Environment validation tests pass</done>
</task>

</tasks>
