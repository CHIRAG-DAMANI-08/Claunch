---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, .eslintrc.json, .prettierrc, .gitignore]
autonomous: true
requirements: [PKG-02, PKG-05]

must_haves:
  truths:
    - "npm install succeeds without errors"
    - "TypeScript compiles without errors"
    - "Vitest runs and finds test files"
    - "ESLint runs without config errors"
    - "Prettier runs without config errors"
  artifacts:
    - package.json
    - tsconfig.json
    - tsup.config.ts
    - vitest.config.ts
  key_links:
    - "package.json type field is 'module' for ESM"
    - "tsconfig.json target is ES2022+ for Node 22"
---

<objective>
Initialize the Node.js TypeScript ESM project with all dev tooling.

Purpose: Establish the build and quality foundation that all subsequent phases depend on.
Output: A working project that compiles TypeScript, runs tests, lints, and formats code.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json with ESM and dev dependencies</name>
  <files>package.json</files>
  <action>
  Create package.json with:
  - name: "claunch"
  - version: "0.1.0"
  - type: "module"
  - bin: { "claunch": "./dist/cli.js" }
  - main: "./dist/index.js"
  - scripts: { "build": "tsup", "dev": "tsup --watch", "test": "vitest run", "test:watch": "vitest", "lint": "eslint src/", "format": "prettier --write src/", "typecheck": "tsc --noEmit" }
  - engines: { "node": ">=22.0.0" }
  - devDependencies: typescript, tsup, vitest, eslint, prettier, @types/node
  - dependencies: commander

  Run `npm install` after creation.
  </action>
  <verify>npm ls --depth=0 shows all packages installed</verify>
  <done>package.json exists with ESM config and all deps installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure TypeScript, tsup, Vitest, ESLint, Prettier</name>
  <files>tsconfig.json, tsup.config.ts, vitest.config.ts, .eslintrc.json, .prettierrc</files>
  <action>
  Create tsconfig.json targeting ES2022, module NodeNext, strict mode, outDir dist/.
  Create tsup.config.ts bundling src/cli.ts as entry, ESM format, banner with shebang.
  Create vitest.config.ts with default config.
  Create .eslintrc.json with TypeScript parser.
  Create .prettierrc with singleQuote, semi, trailingComma all.
  Create .gitignore with node_modules/, dist/, *.tsbuildinfo.
  </action>
  <verify>npx tsc --noEmit succeeds</verify>
  <done>All config files exist and TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder source files and first test</name>
  <files>src/cli.ts, src/index.ts, src/__tests__/placeholder.test.ts</files>
  <action>
  Create src/cli.ts with a minimal Commander setup that just prints version.
  Create src/index.ts that re-exports the main function.
  Create src/__tests__/placeholder.test.ts with a simple passing test.
  Run `npx vitest run` to verify test passes.
  </action>
  <verify>npx vitest run shows 1 test passing</verify>
  <done>Source structure exists and tests pass</done>
</task>

</tasks>
