---
phase: 01-project-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/types/index.ts]
autonomous: true
requirements: [PKG-05]

must_haves:
  truths:
    - "Worktree interface has path, branch, head, isBare, isCurrent fields"
    - "TabSpec interface has path, branch, command, title fields"
    - "SessionData type supports repo-to-branch-to-session mapping"
  artifacts:
    - src/types/index.ts
  key_links:
    - "Types are exported from src/types/index.ts"
---

<objective>
Define core TypeScript interfaces used across all modules.

Purpose: Establish the type contracts that git, claude, terminal, and cli modules depend on.
Output: src/types/index.ts with Worktree, TabSpec, and SessionData types.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define core type interfaces</name>
  <files>src/types/index.ts</files>
  <action>
  Create src/types/index.ts with:
  
  - Worktree interface: { path: string, branch: string, head: string, isBare: boolean, isCurrent: boolean }
  - TabSpec interface: { path: string, branch: string, command: string, title: string }
  - SessionData type: Record<string, Record<string, string>> (repoPath → branch → sessionId)
  - ClaunchError class extending Error with a `code` field for error types
  
  All types exported. TypeScript strict mode compatible.
  </action>
  <verify>npx tsc --noEmit succeeds with the new types</verify>
  <done>Types compile and are importable from src/types/index.ts</done>
</task>

</tasks>
