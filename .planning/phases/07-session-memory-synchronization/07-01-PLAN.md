---
phase: 07-session-memory-synchronization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/cli.ts, src/claude/sessionLog.ts, src/claude/__tests__/sessionLog.test.ts]
autonomous: true
requirements: [SESS-08, SESS-10]

must_haves:
  truths:
    - "TypeScript compiles without errors"
    - "Vitest runs and passes all tests"
  artifacts:
    - src/claude/sessionLog.ts
    - src/claude/__tests__/sessionLog.test.ts
---

<objective>
Implement a subcommand `log` (with alias `session-log`) in the claunch CLI to parse Claude Code's raw JSONL transcripts and print a clean, readable conversation history for any branch.
This allows Claude running in one worktree to query what happened in another session by running a shell command.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement session log parsing and extraction logic</name>
  <files>src/claude/sessionLog.ts</files>
  <action>
  Create src/claude/sessionLog.ts containing:
  - `cleanPathToProjectName(filePath: string): string` replacing all non-alphanumeric characters with `-`.
  - `getSessionLog(repoRoot: string, branchName: string): string` which:
    1. Scans `~/.claude/projects/` directory.
    2. Identifies matching folders for worktrees of the repository.
    3. Finds the latest `.jsonl` files in those folders, prioritizing files matching the target branch name inside the `"gitBranch"` JSON property, or falling back to modified time sorting.
    4. Parses the JSON lines, extracting and formatting the conversation transcript (user prompts and assistant responses) into clean markdown text.
    5. Returns or prints the formatted text.
  </action>
  <verify>Function retrieves and parses JSONL files accurately</verify>
  <done>src/claude/sessionLog.ts is implemented and exports getSessionLog</done>
</task>

<task type="auto">
  <name>Task 2: Wire the log command to Commander.js entry point</name>
  <files>src/cli.ts</files>
  <action>
  Add the `log <branch>` command (with alias `session-log`) to Commander.js configuration in src/cli.ts:
  - Registers the command.
  - Resolves current repo root.
  - Calls `getSessionLog(repoRoot, branch)` and prints the resulting dialogue log to console.
  - Gracefully handles missing logs or missing branch sessions with clear errors.
  </action>
  <verify>claunch log --help shows command description</verify>
  <done>log command is integrated and runnable globally</done>
</task>

<task type="auto">
  <name>Task 3: Implement unit tests for session log parsing</name>
  <files>src/claude/__tests__/sessionLog.test.ts</files>
  <action>
  Create src/claude/__tests__/sessionLog.test.ts:
  - Mock fs operations and home directory.
  - Test correct project directory name generation.
  - Test transcript formatting for user and assistant messages, ensuring tool calls and metadata lines are filtered.
  - Test graceful error behavior for non-existent session directories or logs.
  </action>
  <verify>npm run test passes all tests successfully</verify>
  <done>Unit tests verify session log subcommand functionality</done>
</task>

</tasks>
