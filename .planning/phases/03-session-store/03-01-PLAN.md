---
phase: 03-session-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/claude/sessionStore.ts]
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, SESS-06, SESS-07]

must_haves:
  truths:
    - "SessionStore reads and writes ~/.claunch/sessions.json"
    - "Directory is created lazily on write"
    - "Path keys are normalized"
    - "JSON writes are atomic"
    - "Corrupt JSON resets to empty state"
  artifacts:
    - src/claude/sessionStore.ts
---

<objective>
Implement SessionStore with atomic file operations and path normalization.

Purpose: Persist conversation mapping data safely.
Output: src/claude/sessionStore.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement SessionStore class</name>
  <files>src/claude/sessionStore.ts</files>
  <action>
  Implement src/claude/sessionStore.ts.
  Use os.homedir() to resolve `~/.claunch/sessions.json`.
  Implement normalized paths for keys (lowercase, forward slashes).
  Implement atomic write: write to `.tmp`, then rename to `sessions.json`.
  Implement load logic: if file does not exist or has invalid JSON, fall back to empty `{}` without crashing.
  </action>
  <verify>npx tsc --noEmit compiles cleanly</verify>
  <done>SessionStore compiles</done>
</task>

</tasks>
