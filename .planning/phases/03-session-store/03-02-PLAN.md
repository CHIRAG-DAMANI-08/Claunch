---
phase: 03-session-store
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified: [src/claude/__tests__/sessionStore.test.ts]
autonomous: true
requirements: [SESS-01, SESS-03, SESS-05, SESS-06, SESS-07]

must_haves:
  truths:
    - "SessionStore unit tests pass, verifying atomic writes, path normalization, and corruption fallback"
  artifacts:
    - src/claude/__tests__/sessionStore.test.ts
---

<objective>
Write comprehensive unit tests for SessionStore.

Purpose: Guarantee mapping safety under concurrency and filesystem anomalies.
Output: src/claude/__tests__/sessionStore.test.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add SessionStore unit tests</name>
  <files>src/claude/__tests__/sessionStore.test.ts</files>
  <action>
  Implement src/claude/__tests__/sessionStore.test.ts.
  Test cases:
  - Fresh store load when file does not exist.
  - CRUD operations (get/set/save).
  - Path normalization (matching lowercase and forward slashes).
  - Recovering from invalid/corrupt JSON structure.
  - Directory auto-creation.
  Use fs mock or temp test directories.
  </action>
  <verify>npx vitest run src/claude/ shows all tests passing</verify>
  <done>SessionStore tests pass</done>
</task>

</tasks>
