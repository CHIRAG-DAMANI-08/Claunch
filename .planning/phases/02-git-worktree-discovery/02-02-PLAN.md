---
phase: 02-git-worktree-discovery
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [src/git/__tests__/discoverWorktrees.test.ts]
autonomous: true
requirements: [GIT-02, GIT-03, GIT-04, GIT-05, GIT-06]

must_haves:
  truths:
    - "discoverWorktrees parses normal, bare, detached HEAD, and space-containing paths in mock tests"
  artifacts:
    - src/git/__tests__/discoverWorktrees.test.ts
---

<objective>
Write comprehensive unit tests for discoverWorktrees.

Purpose: Guarantee correct parsing of git porcelain output across all edge cases.
Output: src/git/__tests__/discoverWorktrees.test.ts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add discoverWorktrees unit tests</name>
  <files>src/git/__tests__/discoverWorktrees.test.ts</files>
  <action>
  Create tests in src/git/__tests__/discoverWorktrees.test.ts.
  Mock child_process.execSync to return pre-defined git worktree --porcelain outputs:
  - Normal worktrees
  - Detached HEAD worktrees
  - Bare repositories
  - Paths containing spaces
  - Paths with mismatched case or casing anomalies
  Verify returned attributes for path, branch, head, isBare, isCurrent match mock definitions.
  </action>
  <verify>npx vitest run src/git/ shows all tests passing</verify>
  <done>Worktree discovery tests exist and pass successfully</done>
</task>

</tasks>
