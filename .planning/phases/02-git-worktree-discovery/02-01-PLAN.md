---
phase: 02-git-worktree-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/git/discoverWorktrees.ts]
autonomous: true
requirements: [GIT-01, GIT-02, GIT-03, GIT-04, GIT-05, GIT-06]

must_haves:
  truths:
    - "discoverWorktrees discovers git repo root and lists all worktrees"
    - "Bare worktrees are excluded"
    - "Detached HEAD worktrees use path basename as branch name fallback"
    - "Current worktree is marked with isCurrent: true"
  artifacts:
    - src/git/discoverWorktrees.ts
---

<objective>
Implement git worktree discovery by executing and parsing git commands.

Purpose: Read current worktree states to feed downstream launcher logic.
Output: src/git/discoverWorktrees.ts with discoverWorktrees function.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement discoverWorktrees parser</name>
  <files>src/git/discoverWorktrees.ts</files>
  <action>
  Implement src/git/discoverWorktrees.ts.
  Use child_process.execSync to run:
  - `git rev-parse --show-toplevel` to check we are in a repo.
  - `git worktree list --porcelain` to get the machine-readable worktree list.
  Parse porcelain output blocks:
  - Each block is separated by empty lines.
  - Parse `worktree <path>`, `HEAD <sha>`, and `branch <ref>`.
  - Strip `refs/heads/` prefix from branch names.
  - Detect `bare` flag/property to exclude bare worktrees.
  - If a worktree does not have a `branch` line, it's detached HEAD; fall back to path basename.
  - Match normalized absolute paths against CWD to identify `isCurrent`.
  </action>
  <verify>npx tsc --noEmit compiles cleanly</verify>
  <done>discoverWorktrees compiles and contains parsing logic</done>
</task>

</tasks>
